package parser.lab3;
import java.util.stream.Collectors;
import java.util.Arrays;

public enum Lab3Token {
    INV, FUNC, OP_BR, CL_BR, CURLY_OP_BR, CURLY_CL_BR, VAR_DEC, RETURN, ADD, SUB, MUL, DIV, LESS, GREATER, AND, OR, XOR, ASSIGNMENT, EQUALS, PRINT, IF, ELSE, WHILE, WS, NUM, VAR, END, EPS
}
